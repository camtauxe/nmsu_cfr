<VirtualHost *:80>

    ServerAdmin webmaster@localhost

    DocumentRoot /srv

    Alias /static /srv/static

    WSGIScriptAlias / /srv/wsgi_main.py

    <Directory /srv>
        Require all granted
    </Directory>

    <Directory /srv/utils>
        Require all denied
    </Directory>

</VirtualHost>